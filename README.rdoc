Chat application built in Ruby on Rails 5, where users can join and leave channels along with order crud operations. Messages are sent and recived over actioncable with unread message count and browser based notifications.
